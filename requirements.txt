# VaaniSetu — Python Dependencies
# Install with: pip install -r requirements.txt

# --- Web Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
aiofiles>=23.2.1
sse-starlette>=2.1.0
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 probes its bcrypt backend with a >72-byte password. bcrypt 4.1+
# raises instead of truncating, so startup dies in seed_admin_user() with
# "password cannot be longer than 72 bytes". Pin until passlib ships a fix.
bcrypt<4.1

# --- AI Models ---
faster-whisper>=1.1.0          # CTranslate2 backend, 4-8x faster on CPU + real VAD
# Note: ctranslate2 is installed automatically as a faster-whisper dependency
transformers>=4.40.0
torch>=2.3.0
torchaudio>=2.3.0
sentencepiece>=0.2.0
sacremoses>=0.1.1
accelerate>=0.30.0

# --- TTS ---
gTTS>=2.5.0
piper-tts>=2023.11.14         # ONNX-based, near-real-time on CPU for Indic languages
coqui-tts>=0.27.0; python_version < "3.12"   # XTTS for high-quality voice-clone mode

# --- Document Generation ---
fpdf2>=2.7.9
python-docx>=1.1.2
pypdf>=4.2.0
pysrt>=1.1.2
webvtt-py>=0.4.6

# --- Utilities ---
psutil>=5.9.8
httpx>=0.27.0
librosa>=0.10.0
soundfile>=0.12.0

# --- NLP helpers (for IndicTrans2 preprocessing) ---
indic-nlp-library>=0.92
langdetect>=1.0.9

# --- Data / DB ---
# sqlite3 is built-in; no extra package needed
